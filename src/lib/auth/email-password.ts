/**
 * Local email/password sign-in (this app's Better Auth DB — not the broker).
 *
 * Enabled for self-hosted user registration and sign-in across roles.
 */
export const emailAndPasswordEnabled = true;
